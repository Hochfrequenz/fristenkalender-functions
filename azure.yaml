# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fristenkalender-functions
metadata:
  template: azd-init@1.11.0

# Aspire-based deployment
services:
  app:
    language: dotnet
    project: ./infra/aspire/FristenkalenderFunctions.AppHost/FristenkalenderFunctions.AppHost.csproj
    host: containerapp
